# Cargo build output. Regenerated by `cargo build`; hundreds of MB and machine-specific.
/target
flux_macro/target

# `--emit asm` output from the performance investigation. Reproducible via
# `cargo rustc --release -- --emit asm`; see src/bin/flux_locate.rs.
*.s
